# SwiftUI lab

Newbie track. **One concept per session.** Spec in `concepts/`, implementation in the app (scaffolded when we start this track — not on day one).

Planned builds (map → hands-on):

| Id | Build | Forces |
| --- | --- | --- |
| A | Paginated catalog | async/await, cancel, Observation, NavigationStack, paging, tiny image cache |
| B | Offline notes | SwiftData, search, background save |
| C | Order tracker | State machine, poll mock, UIKit map bridge |
| D | Image gallery | NSCache + disk, downsample, prefetch |
| E | UIKit hybrid | UIHostingController, representable, shared VM |
| F | Stretch | Widget / Live Activity / SPM feature module |

Do **A** first. Starred SwiftUI cards go to `../tracker/super-important/` with `track: swiftui`.
